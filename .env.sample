# Hugging Face token (for model downloads)
# Get yours at: https://huggingface.co/settings/tokens
HF_TOKEN=

# ngrok auth token (optional, for longer Colab sessions)
# Get yours at: https://dashboard.ngrok.com/get-started/your-authtoken
NGROK_AUTH_TOKEN=

# ── External tool paths ──────────────────────────────────────────────────────
# Colab defaults (setup.sh --base-dir /content):
# FRAMEPACK_DIR=/content/FramePack
# CODEFORMER_DIR=/content/CodeFormer
# RIFE_DIR=/content/RIFE

# Docker defaults (setup.sh --base-dir /models):
# FRAMEPACK_DIR=/models/FramePack
# CODEFORMER_DIR=/models/CodeFormer
# RIFE_DIR=/models/RIFE
